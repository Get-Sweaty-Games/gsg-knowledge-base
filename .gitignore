# Claude Code local settings (per-machine only — .claude/skills/ IS committed and shared)
.claude/settings.local.json

# Session handoff notes (Claude Code /handoff) — per-machine working state, not project history
HANDOFF.md

# Node dependencies (not currently used, kept for safety)
node_modules/

# OS files
.DS_Store
Thumbs.db
desktop.ini

# Editor files
*.swp
*.swo
*~
.vscode/settings.json

# Environment files
.env
.env.local

# Cloned reference repos (local only)
*-ref/
